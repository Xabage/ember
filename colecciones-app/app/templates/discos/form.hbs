<h2>{{title}}</h2>

<div class="row">
    <div class="col-md-6">
        {{disco-item-form item=model buttonLabel=buttonLabel action='saveDisco'}}
    </div>

    <div class="col-md-6">
        <img src={{this.model.portada}} alt={{this.model.titulo}} width="300" height="300">
    </div>
</div>