<div class="panel panel-default disco-item">
    <div class="panel-heading">
        <h3 class="panel-title"><b>{{item.grupo}}</b> - {{item.titulo}}</h3>
    </div>
    <div class="panel-body">
        <img class="rounded mx-auto d-block float-center" src={{item.portada}} alt={{item.titulo}}>
        <br>
        <p><b>Formato:</b> {{item.formato}}&nbsp;&nbsp;&nbsp;&nbsp;
            <b>Año:</b> {{item.publicacion}}</p>
    </div>
    <div class="panel-footer text-right">
      {{yield}}
    </div>
</div>